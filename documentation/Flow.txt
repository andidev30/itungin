[service] -> if use [] reference service / service name in cloud run
{ai engine} -> if use {} reference ai engine name in vertex ai
(pub/sub) -> if use () reference pub/sub topic

START

[telegram-listener]
-> on ['text', 'photo', 'document', 'video', 'voice', 'audio']
-> if file size > 5MB -> reply 'file terlalu besar'
-> if video -> reply 'saat ini belum bisa proses video'
-> if document !== pdf -> reply 'saat ini belum bisa proses file selain pdf'
-> if audio -> reply 'saat ini belum bisa proses audio'
-> build body:
headers: {
    apiKey
}
body: {
    userId: ctx.userId,
    text: ctx.text || ctx.caption,
    files: [file voice, file audio, file document] if any
}
-> send to api [message-in] POST /telegram with headers and body

[message-in]
-> in POST /telegram
-> validate apiKey
-> if have files upload all to s3 and return gsUri
-> send to (message-ai)
{
    channelId: ch01,
    userId,
    text,
    files: [gsUri]
}
-> (message-ai) trigger [message-hub]
[message-hub]
-> POST /message
-> use sdk to {itungin-agent}
``

``

END
