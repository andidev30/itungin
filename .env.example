# Itungin Agent Environment Variables

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Use Vertex AI for Gemini
GOOGLE_GENAI_USE_VERTEXAI=True

# MCP Toolbox Configuration
# The MCP Toolbox server must be running separately
# Start it with: cd toolbox-db && ./toolbox --tools-file tools.yaml
MCP_TOOLBOX_URL=http://127.0.0.1:5000

